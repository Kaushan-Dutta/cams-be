version: "3.4"

services:
  postgres:
    image: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: crimewatch_prod
      POSTGRES_PASSWORD: crimewatch_prod

volumes:
  postgres_data:
